#!/bin/bash
# first line is called shebang, used to tell which 
# author: Dominik Duric
# change permissions to execute `chmod 755 `

echo "Hello again `whoami`! Today is the `date`. The bash shell version is ${BASH_VERSION}"
